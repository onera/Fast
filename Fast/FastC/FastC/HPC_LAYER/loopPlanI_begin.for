#ifndef E_SCALAR_COMPUTER
             do k = ind_loop(5), ind_loop(6)
             do j = ind_loop(3), ind_loop(4)

#else
             do k = ind_loop(5), ind_loop(6)
             do j = ind_loop(3), ind_loop(4)
#endif 
               l = inddm(i,j,k)
